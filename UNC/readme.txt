Neural network models are chunked to get around GitHub's file size limit. To reunite a model:

$ cat x*_{model_name} > model_name


SIRMS-based models require the Hit QSAR software, contact Eugene Muratov for access (murik@email.unc.edu)
